import { UnshadowsDirective } from './unshadows.directive';

describe('UnshadowsDirective', () => {
  it('should create an instance', () => {
    const directive = new UnshadowsDirective();
    expect(directive).toBeTruthy();
  });
});
